@using Microsoft.AspNetCore.Http
 
@{
    string Credits = Context.Session.GetString("Credits");
}

@if (!string.IsNullOrEmpty(Credits))
{
    <div class="credits">
        <span>Total Credits: @Credits</span>
    </div>
}